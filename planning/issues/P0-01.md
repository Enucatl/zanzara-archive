<!-- zanzara-plan:P0-01 -->
# [P0-01] Package layout, uv configuration and CPU-only CI

- Stable ID: P0-01
- Executor: Luna
- Kind: Implementation
- Parent: P0
- Blocked by: none

## Outcome and requirement

Existing repository README, license and original plan; output an installable Python 3.14 application skeleton and documented developer commands.

## Design and interfaces

Use [D1](../SYSTEM-DESIGN.md#d1--packages-and-deployment-boundary) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P0](P0.md). No prerequisite issues. This issue does not depend on its own parent closing.

## Bounded steps

1. Create pyproject.toml using uv_build, src/zanzara_archive, a minimal zanzara CLI and committed uv.lock.
2. Configure Ruff, pytest, typed contracts convention and CPU-only GitHub CI with uv sync --frozen. Separate future Python 3.11 service locks from the application.
3. Add synthetic fixture conventions and ignores for private artifacts, credentials, audio, model weights and local databases. Document commands and test markers gpu, integration, browser and paid; paid tests require an explicit opt-in.

## Inputs, outputs and failure behavior

Existing repository README, license and original plan; output an installable Python 3.14 application skeleton and documented developer commands. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Block if Python 3.14 cannot be supported by the application-only dependencies; do not lower the agreed interpreter version.

## Exclusions

No ASR, website features, service image builds or processing of archive audio.

## Acceptance checklist

- [ ] A clean checkout installs with uv sync --frozen and imports the application on Python 3.14.
- [ ] Ruff and the minimal package/CLI smoke test pass without CUDA, model credentials, network inference or archive data.
- [ ] Root README reports foundation status honestly and links the planning entrypoint.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv sync --frozen
uv run ruff check .
uv run ruff format --check .
uv run pytest tests/test_package.py
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues/PRs contain sanitized aggregate results and synthetic examples only. Open a PR describing outcome, checks, limitations and evidence, then mark In review. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Block if Python 3.14 cannot be supported by the application-only dependencies; do not lower the agreed interpreter version. Complete only after the required PR is merged and every acceptance item is satisfied; a draft/open PR alone is not completion. Keep [P0](P0.md) open for Sol review and user release.
