<!-- zanzara-plan:P0-02 -->
# [P0-02] Frozen 20-episode manifest and read-only source validation

- Stable ID: P0-02
- Executor: Luna
- Kind: Implementation
- Parent: P0
- Blocked by: P0-01

## Outcome and requirement

planning/corpus-20.json plus the persistent read-only archive; output the canonical corpus manifest and source-path contract. The archive is trusted after access is provisioned; no recurring source-file verification report is required.

## Design and interfaces

Use [D2](../SYSTEM-DESIGN.md#d2--corpus-and-audio), [D3](../SYSTEM-DESIGN.md#d3--canonical-data-and-publication) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P0](P0.md). Required completed blockers: [P0-01](P0-01.md). This issue does not depend on its own parent closing.

## Bounded steps

1. Import exactly the supplied 20 filenames and metadata; never run a new latest-20 selection.
2. Keep safe root resolution for named sources, rejecting traversal and escaping symlinks when a processing stage opens a source; do not re-hash or re-probe persistent archive files.
3. Document read-only container mounting and local artifact paths; use the manifest's recorded metadata and original time origin without recurring checks.
4. Keep the manifest loader independent of SQLite so this issue can finish before P0-04; P0-04 integrates corpus records into canonical storage.

## Inputs, outputs and failure behavior

planning/corpus-20.json plus the read-only archive; output the canonical corpus import and source-path contract. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when processing cannot open a required source. Archive content is not re-hashed or media-probed as a routine gate.

## Exclusions

No expansion selection, model inference or silent manifest replacement.

## Acceptance checklist

- [ ] Count is 20; date range and golden filename match the planning manifest.
- [ ] Traversal and escaping symlinks fail before a stage opens a source.
- [ ] Source resolution makes no writes to the archive and does not perform recurring content or media checks.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
node planning/validate.mjs
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues and comments contain sanitized aggregate results and synthetic examples only. Commit the bounded change directly to `main`, push `origin/main`, verify the remote commit, and comment with outcome, checks, limitations and evidence. Set this child issue to Done only after all acceptance items pass. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

An unavailable source blocks the stage that needs it; do not manufacture a verification report or substitute synthetic archive files. Complete only after the required commit is pushed to `main` and every acceptance item is satisfied; a local or unpushed commit is not completion. Keep [P0](P0.md) open for Sol review and user release.
