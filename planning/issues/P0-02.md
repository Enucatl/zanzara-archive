<!-- zanzara-plan:P0-02 -->
# [P0-02] Frozen 20-episode manifest and read-only source validation

- Stable ID: P0-02
- Executor: Luna
- Kind: Implementation
- Parent: P0
- Blocked by: P0-01

## Outcome and requirement

planning/corpus-20.json plus the read-only archive; output a verified canonical corpus import and validation report.

## Design and interfaces

Use [D2](../SYSTEM-DESIGN.md#d2--corpus-and-audio), [D3](../SYSTEM-DESIGN.md#d3--canonical-data-and-publication) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P0](P0.md). Required completed blockers: [P0-01](P0-01.md). This issue does not depend on its own parent closing.

## Bounded steps

1. Import exactly the supplied 20 filenames and metadata; never run a new latest-20 selection.
2. Add zanzara corpus verify with manifest validation, safe root resolution, SHA-256 recheck and ffprobe metadata comparison. Detect changed files during reads.
3. Document read-only container mounting and local artifact paths; confirm golden episode checksum and original time origin.
4. Keep the manifest loader and verification CLI independent of SQLite so this issue can finish before P0-04; P0-04 integrates validated corpus records into canonical storage.

## Inputs, outputs and failure behavior

planning/corpus-20.json plus the read-only archive; output a verified canonical corpus import and validation report. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Missing archive or mismatched hash blocks real verification; synthetic tests alone do not close this issue.

## Exclusions

No expansion selection, model inference or silent manifest replacement.

## Acceptance checklist

- [ ] Count is 20; date range and golden filename match the planning manifest.
- [ ] Missing/changed files, traversal and escaping symlinks fail before stage scheduling.
- [ ] Validation makes no writes to the archive and records every file result and manifest hash.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run pytest tests/test_corpus.py
uv run zanzara corpus verify --manifest planning/corpus-20.json --archive-root /export/scratch/archive/zanzara
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues/PRs contain sanitized aggregate results and synthetic examples only. Open a PR describing outcome, checks, limitations and evidence, then mark In review. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Missing archive or mismatched hash blocks real verification; synthetic tests alone do not close this issue. Complete only after the required PR is merged and every acceptance item is satisfied; a draft/open PR alone is not completion. Keep [P0](P0.md) open for Sol review and user release.
