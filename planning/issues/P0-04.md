<!-- zanzara-plan:P0-04 -->
# [P0-04] SQLite migrations, canonical entities and artifact publication

- Stable ID: P0-04
- Executor: Luna
- Kind: Implementation
- Parent: P0
- Blocked by: P0-03

## Outcome and requirement

P0-03 schemas and temporary local directories; output versioned SQLite schema, storage repository and immutable artifact publisher.

## Design and interfaces

Use [D3](../SYSTEM-DESIGN.md#d3--canonical-data-and-publication), [D4](../SYSTEM-DESIGN.md#d4--worker-and-invalidation), [D7](../SYSTEM-DESIGN.md#d7--voice-retrieval-and-identity) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P0](P0.md). Required completed blockers: [P0-03](P0-03.md). This issue does not depend on its own parent closing.

## Bounded steps

1. Implement canonical entities/foreign keys, WAL/FTS5 capability checks and migrations; reject network-backed database/job-state paths.
2. Implement hashed artifact paths, fsync/atomic completed-manifest publication and transactional committed pointers.
3. Represent index generations, identity audit revisions and cost reservations without storing canonical identity solely in Qdrant.

## Inputs, outputs and failure behavior

P0-03 schemas and temporary local directories; output versioned SQLite schema, storage repository and immutable artifact publisher. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Failure to prove local database storage or atomic publication blocks worker integration.

## Exclusions

No separate graph database, shared Qdrant deployment or inference execution.

## Acceptance checklist

- [ ] Fresh migration and upgrade preserve fixture data and enforce foreign keys.
- [ ] Crash between files, manifest rename and SQLite commit never exposes partial output; reconciliation recovers complete orphan artifacts.
- [ ] WAL/SHM, jobs and databases live locally; canonical source remains read-only.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run pytest tests/test_storage.py tests/test_artifacts.py
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues and comments contain sanitized aggregate results and synthetic examples only. Commit the bounded change directly to `main`, push `origin/main`, verify the remote commit, and comment with outcome, checks, limitations and evidence. Set this child issue to Done only after all acceptance items pass. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Failure to prove local database storage or atomic publication blocks worker integration. Complete only after the required commit is pushed to `main` and every acceptance item is satisfied; a local or unpushed commit is not completion. Keep [P0](P0.md) open for Sol review and user release.
