<!-- zanzara-plan:P1-02 -->
# [P1-02] Parakeet service and genuine word timestamps

- Stable ID: P1-02
- Executor: Luna
- Kind: Implementation
- Parent: P1
- Blocked by: P0, P1-01

## Outcome and requirement

Locked Parakeet image and audio artifacts; output local transcription endpoint, adapter and timed-word stage.

## Design and interfaces

Use [D2](../SYSTEM-DESIGN.md#d2--corpus-and-audio), [D5](../SYSTEM-DESIGN.md#d5--models-and-adapters), [D6](../SYSTEM-DESIGN.md#d6--transcript-production) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P1](P1.md). Required completed blockers: [P0](P0.md), [P1-01](P1-01.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Implement the JSON local transcription subset described in D8 and declare actual word-timestamp support.
2. Infer 300-second ownership windows with five-second context, convert offsets once and apply deterministic midpoint ownership.
3. Persist raw/private results, original offsets, model/configuration fingerprints and typed errors; support restart through worker artifacts.

## Inputs, outputs and failure behavior

Locked Parakeet image and audio artifacts; output local transcription endpoint, adapter and timed-word stage. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Missing real timestamps or GPU compatibility regression blocks publication.

## Exclusions

No diarization, automatic identity labels or cloud spending.

## Acceptance checklist

- [ ] Known boundary fixtures contain neither duplicated context words nor missing owned words, including real repeated tokens.
- [ ] Real golden inference returns genuine bounded monotonic word times with no interpolation.
- [ ] Production rejects absent timestamps and reports unsupported capability clearly.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run pytest tests/test_parakeet_adapter.py tests/test_asr_boundaries.py
uv run zanzara process --manifest planning/corpus-20.json --episode 260910-lazanzara.opus --stage asr
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues/PRs contain sanitized aggregate results and synthetic examples only. Open a PR describing outcome, checks, limitations and evidence, then mark In review. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Missing real timestamps or GPU compatibility regression blocks publication. Complete only after the required PR is merged and every acceptance item is satisfied; a draft/open PR alone is not completion. Keep [P1](P1.md) open for Sol review and user release.
