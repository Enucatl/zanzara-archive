<!-- zanzara-plan:P1-06 -->
# [P1-06] Human: review golden transcript and freeze evaluation split

- Stable ID: P1-06
- Executor: Human
- Kind: Operator
- Parent: P1
- Blocked by: P0, P1-05

## Outcome and requirement

Local annotation UI and complete golden audio; output a human-reviewed verbatim reference and frozen split/timing manifests.

## Design and interfaces

Use [E1](../EVALUATION.md#e1--golden-reference-and-transcription-split), [E2](../EVALUATION.md#e2--asr-diarization-attribution-and-timing-metrics) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P1](P1.md). Required completed blockers: [P0](P0.md), [P1-05](P1-05.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Listen through the entire episode and correct Italian words, speaker turns and overlaps; explicitly mark unintelligible regions.
2. Verify start/end boundaries for at least 200 distinct words across the five blocks.
3. Freeze 80/20 contiguous development/held-out boundaries, slice annotations and it-v1 normalization before any comparison; record reviewer/version/hash.

## Inputs, outputs and failure behavior

Local annotation UI and complete golden audio; output a human-reviewed verbatim reference and frozen split/timing manifests. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Incomplete listening, missing timing sample or unreviewed spans keep this Human task open.

## Exclusions

No Luna self-review, metric tuning on held-out data or replacement golden episode.

## Acceptance checklist

- [ ] The entire golden episode has a reviewed state and auditable annotation history.
- [ ] At least 200 timed words and all five contiguous split blocks are exported with checksums.
- [ ] Held-out handling and exclusions are documented; machine seeds are not passed off as human truth.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run zanzara evaluation validate-reference --corpus planning/corpus-20.json --reference "$ZANZARA_REFERENCE"
```

The operator performs the action/review; CLI validation checks format and completeness, never substitutes for human judgment. If a required validation command is missing, request a bounded Luna follow-up rather than pretending this Human task implemented it.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues and comments contain sanitized aggregate results and synthetic examples only. Include the human actor, action date and explicit decision; never publish credentials or private annotations.

## Stop conditions and completion rule

Incomplete listening, missing timing sample or unreviewed spans keep this Human task open. Complete only when the human action and acceptance evidence are recorded. Keep [P1](P1.md) open for Sol review and user release.
