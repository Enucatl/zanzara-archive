<!-- zanzara-plan:P1-07 -->
# [P1-07] ASR, diarization, attribution and timing evaluation harness

- Stable ID: P1-07
- Executor: Luna
- Kind: Evaluation
- Parent: P1
- Blocked by: P0, P1-05

## Outcome and requirement

Annotation export schema and known synthetic references/hypotheses; output reproducible metric CLI and report artifacts.

## Design and interfaces

Use [E1](../EVALUATION.md#e1--golden-reference-and-transcription-split), [E2](../EVALUATION.md#e2--asr-diarization-attribution-and-timing-metrics), [E6](../EVALUATION.md#e6--operational-measurements-and-report-format) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P1](P1.md). Required completed blockers: [P0](P0.md), [P1-05](P1-05.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Implement raw/normalized WER, CER, primary/secondary DER, JER, count error, SA-WER, unassigned rate and timing quantiles with versioned definitions.
2. Add manually calculated fixtures for substitutions/deletions/insertions, overlap, speaker confusion, timing coverage and unintelligible masks.
3. Write private machine-readable results and aggregate HTML with hashes, denominators, slices and pass/block logic.

## Inputs, outputs and failure behavior

Annotation export schema and known synthetic references/hypotheses; output reproducible metric CLI and report artifacts. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Unspecified metric variant or unverified known-answer output blocks harness acceptance.

## Exclusions

No human reference fabrication, actual baseline verdict or threshold relaxation.

## Acceptance checklist

- [ ] Known-answer metrics pass without model downloads or paid calls.
- [ ] Zero-denominator/missing reference states are insufficient evidence, not success.
- [ ] Timing seconds conversion and split masks do not leak into persisted millisecond records.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run pytest tests/test_evaluation_asr.py tests/test_evaluation_diarization.py tests/test_evaluation_timing.py
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues and comments contain sanitized aggregate results and synthetic examples only. Commit the bounded change directly to `main`, push `origin/main`, verify the remote commit, and comment with outcome, checks, limitations and evidence. Set this child issue to Done only after all acceptance items pass. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Unspecified metric variant or unverified known-answer output blocks harness acceptance. Complete only after the required commit is pushed to `main` and every acceptance item is satisfied; a local or unpushed commit is not completion. Keep [P1](P1.md) open for Sol review and user release.
