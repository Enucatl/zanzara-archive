<!-- zanzara-plan:P2-01 -->
# [P2-01] Deterministic clean-exemplar extraction

- Stable ID: P2-01
- Executor: Luna
- Kind: Implementation
- Parent: P2
- Blocked by: P1

## Outcome and requirement

Frozen 20-episode manifest and diarization stage; output clean exemplar manifests for all episode speakers.

## Design and interfaces

Use [D2](../SYSTEM-DESIGN.md#d2--corpus-and-audio), [D7](../SYSTEM-DESIGN.md#d7--voice-retrieval-and-identity) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P2](P2.md). Required completed blockers: [P1](P1.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Process diarization for the remaining frozen episodes as needed; do not require ASR.
2. Subtract overlap/transition margins, select 8–15 second preferred or >=3 second fallback excerpts and retain at most ten using D7 ordering.
3. Record quality measurements, exact original timestamps, rejection reasons and explicit not_voice_searchable status.

## Inputs, outputs and failure behavior

Frozen 20-episode manifest and diarization stage; output clean exemplar manifests for all episode speakers. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Missing diarization or unsafe source offsets block affected artifacts; insufficient clean speech is a recorded status, not a dropped speaker.

## Exclusions

No ASR requirement, fabricated model confidence or enrollment decisions.

## Acceptance checklist

- [ ] Selection is deterministic across reruns and extracts no overlap or 250 ms transition margin.
- [ ] Boundary/short-speech fixtures enforce duration/count limits and preserve unsearchable speakers.
- [ ] Real exemplar coverage is accounted for across all 20 episodes.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run pytest tests/test_exemplars.py
uv run zanzara process --manifest planning/corpus-20.json --stage exemplars
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues and comments contain sanitized aggregate results and synthetic examples only. Commit the bounded change directly to `main`, push `origin/main`, verify the remote commit, and comment with outcome, checks, limitations and evidence. Set this child issue to Done only after all acceptance items pass. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Missing diarization or unsafe source offsets block affected artifacts; insufficient clean speech is a recorded status, not a dropped speaker. Complete only after the required commit is pushed to `main` and every acceptance item is satisfied; a local or unpushed commit is not completion. Keep [P2](P2.md) open for Sol review and user release.
