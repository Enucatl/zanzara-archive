<!-- zanzara-plan:P2-02 -->
# [P2-02] ResNet293 service adapter and validated embedding fixtures

- Stable ID: P2-02
- Executor: Luna
- Kind: Implementation
- Parent: P2
- Blocked by: P1

## Outcome and requirement

P1-01 model lock for Wespeaker/wespeaker-voxceleb-resnet293-LM, synthetic and private clean excerpts; output independently locked resnet293 service and SpeakerEmbedder adapter.

## Design and interfaces

Use [D5](../SYSTEM-DESIGN.md#d5--models-and-adapters), [D7](../SYSTEM-DESIGN.md#d7--voice-retrieval-and-identity) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P2](P2.md). Required completed blockers: [P1](P1.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Implement resnet293 ordered-batch inference using exactly its locked preprocessing, checkpoint and dimension.
2. Validate checkpoint loading and official feature extraction conventions against the acquisition smoke result.
3. Reject invalid/short audio and non-finite, zero or wrong-dimensional vectors; persist original vectors and model/config hashes.

## Inputs, outputs and failure behavior

P1-01 model lock for Wespeaker/wespeaker-voxceleb-resnet293-LM, synthetic and private clean excerpts; output independently locked resnet293 service and SpeakerEmbedder adapter. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Missing checkpoint/head, dimension mismatch or failed GPU inference blocks integration.

## Exclusions

No alternative checkpoints, acoustic identity decisions or silent two-model ensemble.

## Acceptance checklist

- [ ] CPU contract fixtures preserve ordering and fail incompatible fingerprints.
- [ ] Real RTX 5090 excerpt inference produces repeatable finite vectors within a recorded numerical tolerance for ResNet293.
- [ ] A changed preprocessing/model fingerprint cannot write into an old vector generation.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run pytest tests/test_resnet293_adapter.py
docker compose --profile processing run --rm resnet293-smoke
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues/PRs contain sanitized aggregate results and synthetic examples only. Open a PR describing outcome, checks, limitations and evidence, then mark In review. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Missing checkpoint/head, dimension mismatch or failed GPU inference blocks integration. Complete only after the required PR is merged and every acceptance item is satisfied; a draft/open PR alone is not completion. Keep [P2](P2.md) open for Sol review and user release.
