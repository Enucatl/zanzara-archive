<!-- zanzara-plan:P2-09 -->
# [P2-09] Audited manual identity decisions, undo and split

- Stable ID: P2-09
- Executor: Luna
- Kind: Implementation
- Parent: P2
- Blocked by: P1, P2-06

## Outcome and requirement

Candidate provenance and canonical SQLite speakers; output transactional identity API and review history.

## Design and interfaces

Use [D3](../SYSTEM-DESIGN.md#d3--canonical-data-and-publication), [D7](../SYSTEM-DESIGN.md#d7--voice-retrieval-and-identity), [D10](../SYSTEM-DESIGN.md#d10--website-and-api) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P2](P2.md). Required completed blockers: [P1](P1.md), [P2-06](P2-06.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Implement human same-person/different-person/uncertain decisions, manual names and revision-checked updates.
2. Reject proposed merges that contradict any active different-person edge across resulting clusters, including transitive conflicts.
3. Implement auditable undo and explicit cluster splitting with superseded edges; keep re-diarized mappings stale pending review.

## Inputs, outputs and failure behavior

Candidate provenance and canonical SQLite speakers; output transactional identity API and review history. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Any irreversible correction or contradiction bypass blocks acceptance.

## Exclusions

No automatic clustering, deletion of decision history or real-name inference.

## Acceptance checklist

- [ ] Merge/reject/uncertain/undo/split fixtures preserve full history and expected appearances.
- [ ] Contradictory and stale updates fail atomically without partial membership changes.
- [ ] Names remain separate from acoustic membership; no model score directly merges identities.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run pytest tests/test_identity.py tests/test_identity_api.py
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues/PRs contain sanitized aggregate results and synthetic examples only. Open a PR describing outcome, checks, limitations and evidence, then mark In review. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Any irreversible correction or contradiction bypass blocks acceptance. Complete only after the required PR is merged and every acceptance item is satisfied; a draft/open PR alone is not completion. Keep [P2](P2.md) open for Sol review and user release.
