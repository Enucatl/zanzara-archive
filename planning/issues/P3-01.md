<!-- zanzara-plan:P3-01 -->
# [P3-01] Upstream shared-inference transcription and pinned integration

- Stable ID: P3-01
- Executor: Luna
- Kind: Implementation
- Parent: P3
- Blocked by: P2

## Outcome and requirement

shared-inference inspected commit and current upstream code; output independently tested upstream PR/merged revision and archive pin.

## Design and interfaces

Use [D5](../SYSTEM-DESIGN.md#d5--models-and-adapters), [D8](../SYSTEM-DESIGN.md#d8--shared-inference-provider-changes-and-money) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P3](P3.md). Required completed blockers: [P2](P2.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Re-read InferenceClient, models/errors/tracing upstream and add the bounded typed transcribe API with audio bytes, format, language, timestamp requirements and provider options.
2. Support OpenRouter JSON and the equivalent local contract, preserving usage/request ID/raw result and typed failure/tracing conventions.
3. Test upstream with local/remote response fixtures including text-only capability; after merge pin the archive lock to the merged SHA and integrate Transcriber.

## Inputs, outputs and failure behavior

shared-inference inspected commit and current upstream code; output independently tested upstream PR/merged revision and archive pin. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Upstream merge/access unavailable keeps integration open; prepare a reviewable PR and report the exact blocker.

## Exclusions

No unrelated shared-inference refactor, floating branch pin or fabricated timing.

## Acceptance checklist

- [ ] Upstream tests cover optional words/segments, provider options, errors, request ID/usage and tracing without real paid calls.
- [ ] Archive production attribution rejects text-only output while WER evaluation accepts it.
- [ ] Archive dependency resolves to the actual merged upstream revision.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

In upstream checkout: uv run ruff check .; uv run ruff format --check .; uv run pytest
In archive: uv sync --frozen; uv run pytest tests/test_shared_transcription.py

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues/PRs contain sanitized aggregate results and synthetic examples only. Open a PR describing outcome, checks, limitations and evidence, then mark In review. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Upstream merge/access unavailable keeps integration open; prepare a reviewable PR and report the exact blocker. Complete only after the required PR is merged and every acceptance item is satisfied; a draft/open PR alone is not completion. Keep [P3](P3.md) open for Sol review and user release.
