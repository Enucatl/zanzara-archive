<!-- zanzara-plan:P3-02 -->
# [P3-02] Local BGE-M3 and shared-inference embedding adapter

- Stable ID: P3-02
- Executor: Luna
- Kind: Implementation
- Parent: P3
- Blocked by: P2

## Outcome and requirement

Locked BGE-M3 model and existing shared-inference embed API; output local compatible endpoint and ordered text adapter.

## Design and interfaces

Use [D1](../SYSTEM-DESIGN.md#d1--packages-and-deployment-boundary), [D5](../SYSTEM-DESIGN.md#d5--models-and-adapters), [D8](../SYSTEM-DESIGN.md#d8--shared-inference-provider-changes-and-money) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P3](P3.md). Required completed blockers: [P2](P2.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Serve dense 1,024-dimensional BGE-M3 embeddings from a separately locked Python 3.11 container.
2. Use shared-inference InferenceClient.embed in the application and validate response index ordering against input strings.
3. Record tokenization/preprocessing/revision fingerprint, reject truncation/invalid vectors and expose readiness failures.

## Inputs, outputs and failure behavior

Locked BGE-M3 model and existing shared-inference embed API; output local compatible endpoint and ordered text adapter. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Unsupported runtime or ambiguous embedding ordering prevents indexing.

## Exclusions

No sparse/ColBERT outputs, reranker or cloud spend.

## Acceptance checklist

- [ ] Italian text fixtures return ordered finite 1,024-dimensional dense vectors through the actual GPU endpoint.
- [ ] CPU fixtures catch reordered responses, wrong dimensions and incompatible fingerprints.
- [ ] Application remains Python 3.14 without importing ML packages.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run pytest tests/test_text_embedder.py
docker compose --profile serving run --rm text-embeddings-smoke
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues and comments contain sanitized aggregate results and synthetic examples only. Commit the bounded change directly to `main`, push `origin/main`, verify the remote commit, and comment with outcome, checks, limitations and evidence. Set this child issue to Done only after all acceptance items pass. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Unsupported runtime or ambiguous embedding ordering prevents indexing. Complete only after the required commit is pushed to `main` and every acceptance item is satisfied; a local or unpushed commit is not completion. Keep [P3](P3.md) open for Sol review and user release.
