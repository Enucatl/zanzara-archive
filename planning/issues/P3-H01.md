<!-- zanzara-plan:P3-H01 -->
# [P3-H01] Operator: provision capped OpenRouter benchmark key

- Stable ID: P3-H01
- Executor: Human
- Kind: Operator
- Parent: P3
- Blocked by: P2

## Outcome and requirement

Initial US$10 benchmark policy; output privately configured capped key and dated balance evidence.

## Design and interfaces

Use [D8](../SYSTEM-DESIGN.md#d8--shared-inference-provider-changes-and-money), [E5](../EVALUATION.md#e5--localcloud-comparisons-and-budget) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P3](P3.md). Required completed blockers: [P2](P2.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Create/configure an OpenRouter key with non-resetting US$10 credit limit and enough available balance for reserved estimates.
2. Provide the key via private environment/secret file and confirm allowed benchmark providers; never paste it in GitHub.
3. Record key-cap/balance verification time and operator action without credential values.

## Inputs, outputs and failure behavior

Initial US$10 benchmark policy; output privately configured capped key and dated balance evidence. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Missing cap, credential or balance blocks paid capability probes.

## Exclusions

No uncapped key, recurring budget reset or authorization for expansion spending.

## Acceptance checklist

- [ ] The dedicated key cap is US$10 and credential location is usable by the controlled benchmark process.
- [ ] No paid call is made as part of provisioning; the local ledger still gates every later attempt.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

Operator evidence: redacted cap/balance confirmation and private environment configured.

The operator performs the action/review; CLI validation checks format and completeness, never substitutes for human judgment. If a required validation command is missing, request a bounded Luna follow-up rather than pretending this Human task implemented it.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues and comments contain sanitized aggregate results and synthetic examples only. Include the human actor, action date and explicit decision; never publish credentials or private annotations.

## Stop conditions and completion rule

Missing cap, credential or balance blocks paid capability probes. Complete only when the human action and acceptance evidence are recorded. Keep [P3](P3.md) open for Sol review and user release.
