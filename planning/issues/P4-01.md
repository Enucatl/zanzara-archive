<!-- zanzara-plan:P4-01 -->
# [P4-01] Responsive application shell and API integration

- Stable ID: P4-01
- Executor: Luna
- Kind: Implementation
- Parent: P4
- Blocked by: P3

## Outcome and requirement

Existing local tooling and /api/v1 contracts; output accessible Jinja navigation/layout and API client helpers.

## Design and interfaces

Use [D1](../SYSTEM-DESIGN.md#d1--packages-and-deployment-boundary), [D10](../SYSTEM-DESIGN.md#d10--website-and-api), [D11](../SYSTEM-DESIGN.md#d11--access-and-operational-profiles) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P4](P4.md). Required completed blockers: [P3](P3.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Build responsive shared templates and navigation for search, episodes, voices, identities and evaluations.
2. Add modest vanilla JavaScript helpers for polling/playback/forms with typed error states and CSRF-ready mutations.
3. Use loopback-only development deployment pending P5 Access enforcement; render synthetic demo data for browser fixtures.

## Inputs, outputs and failure behavior

Existing local tooling and /api/v1 contracts; output accessible Jinja navigation/layout and API client helpers. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Accidental external exposure or incompatible API contracts block acceptance.

## Exclusions

No public tunnel, frontend framework migration or speculative dashboard features.

## Acceptance checklist

- [ ] Keyboard/mobile navigation and server-rendered fallback work without a frontend framework.
- [ ] API failures are understandable and no private service endpoints/credentials are exposed to browser code.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run pytest tests/test_web_shell.py
uv run pytest -m browser tests/browser/test_navigation.py
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues and comments contain sanitized aggregate results and synthetic examples only. Commit the bounded change directly to `main`, push `origin/main`, verify the remote commit, and comment with outcome, checks, limitations and evidence. Set this child issue to Done only after all acceptance items pass. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Accidental external exposure or incompatible API contracts block acceptance. Complete only after the required commit is pushed to `main` and every acceptance item is satisfied; a local or unpushed commit is not completion. Keep [P4](P4.md) open for Sol review and user release.
