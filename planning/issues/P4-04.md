<!-- zanzara-plan:P4-04 -->
# [P4-04] Speaker comparison, identity review and chronology

- Stable ID: P4-04
- Executor: Luna
- Kind: Implementation
- Parent: P4
- Blocked by: P3, P4-01

## Outcome and requirement

Candidate and identity APIs; output human comparison/history/appearance pages.

## Design and interfaces

Use [D7](../SYSTEM-DESIGN.md#d7--voice-retrieval-and-identity), [D10](../SYSTEM-DESIGN.md#d10--website-and-api) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P4](P4.md). Required completed blockers: [P3](P3.md), [P4-01](P4-01.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Provide side-by-side representative audio with per-model scores and provenance, distinguishing uncalibrated scores.
2. Expose same/different/uncertain decisions, manual names, undo/split and explicit contradiction/stale-update errors.
3. Show global appearances, anonymous recurrence and chronological first/last appearances, including voice-only ASR-missing entries.

## Inputs, outputs and failure behavior

Candidate and identity APIs; output human comparison/history/appearance pages. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Misleading probability labels, hidden contradiction errors or unreversible UI changes block completion.

## Exclusions

No demographic inference, automatic merges or edit-history deletion.

## Acceptance checklist

- [ ] Human review journeys preserve auditable identity changes and support reversal.
- [ ] No candidate score automatically confirms a person or assigns a real name.
- [ ] Chronology reflects canonical current membership after merge/undo/split.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run pytest -m browser tests/browser/test_identity_review.py
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues and comments contain sanitized aggregate results and synthetic examples only. Commit the bounded change directly to `main`, push `origin/main`, verify the remote commit, and comment with outcome, checks, limitations and evidence. Set this child issue to Done only after all acceptance items pass. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Misleading probability labels, hidden contradiction errors or unreversible UI changes block completion. Complete only after the required commit is pushed to `main` and every acceptance item is satisfied; a local or unpushed commit is not completion. Keep [P4](P4.md) open for Sol review and user release.
