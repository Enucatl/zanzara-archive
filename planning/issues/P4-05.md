<!-- zanzara-plan:P4-05 -->
# [P4-05] Evaluation dashboard and job/error states

- Stable ID: P4-05
- Executor: Luna
- Kind: Implementation
- Parent: P4
- Blocked by: P3, P4-01

## Outcome and requirement

Private report metadata and job APIs; output report dashboard and actionable state presentation.

## Design and interfaces

Use [D10](../SYSTEM-DESIGN.md#d10--website-and-api), [E6](../EVALUATION.md#e6--operational-measurements-and-report-format) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P4](P4.md). Required completed blockers: [P3](P3.md), [P4-01](P4-01.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Render quality/coverage/latency/cost with source/model/split/commit context and pass/block status.
2. Expose queue/running/retry/failed/blocked/cancelled/expired states with useful actions and missing-resource reasons.
3. Separate synthetic demos, measured reports, development and held-out metrics; show missing evidence explicitly.

## Inputs, outputs and failure behavior

Private report metadata and job APIs; output report dashboard and actionable state presentation. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Misrepresented evidence or bypassable job gates block acceptance.

## Exclusions

No invented metrics, blanket retry buttons that bypass ledger or public raw reports.

## Acceptance checklist

- [ ] Reports expose denominators, limitations and uncalibrated/insufficient evidence states.
- [ ] Polling stops appropriately at terminal/expired states and retries remain budget-controlled.
- [ ] Private raw artifacts are not leaked into public assets or logs.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run pytest -m browser tests/browser/test_evaluation_jobs.py
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues/PRs contain sanitized aggregate results and synthetic examples only. Open a PR describing outcome, checks, limitations and evidence, then mark In review. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Misrepresented evidence or bypassable job gates block acceptance. Complete only after the required PR is merged and every acceptance item is satisfied; a draft/open PR alone is not completion. Keep [P4](P4.md) open for Sol review and user release.
