<!-- zanzara-plan:P4-06 -->
# [P4-06] Browser tests for complete user journeys

- Stable ID: P4-06
- Executor: Luna
- Kind: Evaluation
- Parent: P4
- Blocked by: P3, P4-02, P4-03, P4-04, P4-05

## Outcome and requirement

Integrated website and deterministic synthetic services; output end-to-end browser suite and journey evidence.

## Design and interfaces

Use [D10](../SYSTEM-DESIGN.md#d10--website-and-api), [D11](../SYSTEM-DESIGN.md#d11--access-and-operational-profiles), [E7](../EVALUATION.md#e7--gates-and-expansion-checks) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P4](P4.md). Required completed blockers: [P3](P3.md), [P4-02](P4-02.md), [P4-03](P4-03.md), [P4-04](P4-04.md), [P4-05](P4-05.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Exercise text search/filter → excerpt → word playback; archive/upload voice → candidate comparison; decision → contradiction → undo/split.
2. Exercise lexical degradation, multiple-speaker selection, upload expiry, failed/retried jobs and evaluation report states.
3. Run representative desktop/mobile keyboard journeys; distinguish synthetic integration checks from real model validation already recorded.

## Inputs, outputs and failure behavior

Integrated website and deterministic synthetic services; output end-to-end browser suite and journey evidence. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Any broken required journey creates bounded remediation and blocks P4.

## Exclusions

No full new feature work, model quality replacement by mocks or phase release.

## Acceptance checklist

- [ ] Complete journeys pass in a clean local Compose/test environment.
- [ ] Browser tests assert visible behavior and relevant media timing, not only DOM snapshots.
- [ ] No test fixture permits real paid calls or exposes the website before Access.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run pytest -m browser tests/browser
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues and comments contain sanitized aggregate results and synthetic examples only. Commit the bounded change directly to `main`, push `origin/main`, verify the remote commit, and comment with outcome, checks, limitations and evidence. Set this child issue to Done only after all acceptance items pass. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Any broken required journey creates bounded remediation and blocks P4. Complete only after the required commit is pushed to `main` and every acceptance item is satisfied; a local or unpushed commit is not completion. Keep [P4](P4.md) open for Sol review and user release.
