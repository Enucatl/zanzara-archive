<!-- zanzara-plan:P6-01 -->
# [P6-01] Freeze 400 recent episodes and incremental estimates

- Stable ID: P6-01
- Executor: Luna
- Kind: Implementation
- Parent: P6
- Blocked by: P5

## Outcome and requirement

Released P5, archive metadata and measured throughput/storage; output frozen nested 400 manifest and resource estimate.

## Design and interfaces

Use [D2](../SYSTEM-DESIGN.md#d2--corpus-and-audio), [D12](../SYSTEM-DESIGN.md#d12--expansion-and-documentation), [E7](../EVALUATION.md#e7--gates-and-expansion-checks) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P6](P6.md). Required completed blockers: [P5](P5.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Select 400 newest episodes at the initial 2026-09-10 cutoff, including the frozen 20, and lock hashes/media metadata.
2. Choose the 40-total canary as the original 20 plus the next 20 by date; identify reusable artifacts and incremental work.
3. Estimate additional stage hours, disk/backup/cache needs and uncertainty from real P5 measurements; record no paid expansion inference.

## Inputs, outputs and failure behavior

Released P5, archive metadata and measured throughput/storage; output frozen nested 400 manifest and resource estimate. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Missing source files, <400 eligible entries or insufficient storage evidence blocks the expansion proposal.

## Exclusions

No processing canary/bulk before P6-02 or new initial benchmark selection.

## Acceptance checklist

- [ ] 400 unique source entries include the initial 20 unchanged and the 40-total canary is explicit.
- [ ] Estimates subtract reusable outputs and expose storage/throughput assumptions.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run zanzara corpus freeze-expansion --base planning/corpus-20.json --count 400 --cutoff 2026-09-10 --output "$ZANZARA_CORPUS_400"
uv run zanzara estimate --manifest "$ZANZARA_CORPUS_400"
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues and comments contain sanitized aggregate results and synthetic examples only. Commit the bounded change directly to `main`, push `origin/main`, verify the remote commit, and comment with outcome, checks, limitations and evidence. Set this child issue to Done only after all acceptance items pass. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Missing source files, <400 eligible entries or insufficient storage evidence blocks the expansion proposal. Complete only after the required commit is pushed to `main` and every acceptance item is satisfied; a local or unpushed commit is not completion. Keep [P6](P6.md) open for Sol review and user release.
