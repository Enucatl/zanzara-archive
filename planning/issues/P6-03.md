<!-- zanzara-plan:P6-03 -->
# [P6-03] Process 40-total canary and check drift/regressions

- Stable ID: P6-03
- Executor: Luna
- Kind: Evaluation
- Parent: P6
- Blocked by: P5, P6-02

## Outcome and requirement

Approved 400 manifest and 40-total canary; output processed canary and reviewed drift/quality evidence.

## Design and interfaces

Use [D12](../SYSTEM-DESIGN.md#d12--expansion-and-documentation), [E7](../EVALUATION.md#e7--gates-and-expansion-checks) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P6](P6.md). Required completed blockers: [P5](P5.md), [P6-02](P6-02.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Process only missing artifacts needed to reach 40 total episodes; verify all expected text/voice stages.
2. Obtain human review of representative new transcript/turn/identity samples via explicit Human follow-up if not already supplied; freeze canary references before scoring.
3. Run relevant existing acceptance thresholds and compare quality/coverage/resource distributions; publish a pass/block report.

## Inputs, outputs and failure behavior

Approved 400 manifest and 40-total canary; output processed canary and reviewed drift/quality evidence. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Failed quality/drift checks or pending human annotations keep this issue and bulk processing blocked.

## Exclusions

No remaining bulk run, synthetic substitutes for human canary labels or relaxed targets.

## Acceptance checklist

- [ ] Canary contains 40 total episodes, not 40 additional episodes.
- [ ] Human-reviewed new material and frozen baseline checks support the quality verdict.
- [ ] Relevant E7 gates pass before P6-04 becomes eligible.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run zanzara process --manifest "$ZANZARA_CANARY_40" --stage all
uv run zanzara acceptance run --manifest "$ZANZARA_CANARY_40" --output "$ZANZARA_CANARY_REPORT"
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues/PRs contain sanitized aggregate results and synthetic examples only. Open a PR describing outcome, checks, limitations and evidence, then mark In review. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Failed quality/drift checks or pending human annotations keep this issue and bulk processing blocked. Complete only after the required PR is merged and every acceptance item is satisfied; a draft/open PR alone is not completion. Keep [P6](P6.md) open for Sol review and user release.
