<!-- zanzara-plan:P6-04 -->
# [P6-04] Process remaining 400 manifest and publish coverage

- Stable ID: P6-04
- Executor: Luna
- Kind: Implementation
- Parent: P6
- Blocked by: P5, P6-03

## Outcome and requirement

Passed 40-total canary and approved 400 manifest; output complete recent-400 searchable corpus and report.

## Design and interfaces

Use [D4](../SYSTEM-DESIGN.md#d4--worker-and-invalidation), [D12](../SYSTEM-DESIGN.md#d12--expansion-and-documentation), [E7](../EVALUATION.md#e7--gates-and-expansion-checks) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P6](P6.md). Required completed blockers: [P5](P5.md), [P6-03](P6-03.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Resume missing stage jobs for remaining manifest entries using the measured profile and bounded retries.
2. Verify per-stage coverage, explicit voice-unsearchable statuses, index consistency and current regression evidence.
3. Publish resource/error/coverage report and request integrated Sol review without closing P6.

## Inputs, outputs and failure behavior

Passed 40-total canary and approved 400 manifest; output complete recent-400 searchable corpus and report. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Unresolved stage failures, regression or resource exhaustion blocks completeness.

## Exclusions

No historical processing or phase release.

## Acceptance checklist

- [ ] All 400 entries are accounted for with complete required artifacts or explicit unresolved blockers.
- [ ] No canary failures are bypassed and reprocessing does not duplicate indexes.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run zanzara process --manifest "$ZANZARA_CORPUS_400" --stage all
uv run zanzara acceptance run --manifest "$ZANZARA_CORPUS_400" --output "$ZANZARA_EXPANSION_REPORT"
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues/PRs contain sanitized aggregate results and synthetic examples only. Open a PR describing outcome, checks, limitations and evidence, then mark In review. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Unresolved stage failures, regression or resource exhaustion blocks completeness. Complete only after the required PR is merged and every acceptance item is satisfied; a draft/open PR alone is not completion. Keep [P6](P6.md) open for Sol review and user release.
