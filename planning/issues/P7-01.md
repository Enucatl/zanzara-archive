<!-- zanzara-plan:P7-01 -->
# [P7-01] Freeze historical manifest and execute stratified voice canary

- Stable ID: P7-01
- Executor: Luna
- Kind: Implementation
- Parent: P7
- Blocked by: P6

## Outcome and requirement

Released P6 and remaining archive; output historical manifest, stratified 20 canary, private voice artifacts and estimates.

## Design and interfaces

Use [D2](../SYSTEM-DESIGN.md#d2--corpus-and-audio), [D12](../SYSTEM-DESIGN.md#d12--expansion-and-documentation), [E7](../EVALUATION.md#e7--gates-and-expansion-checks) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P7](P7.md). Required completed blockers: [P6](P6.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Freeze remaining episodes excluding indexed corpus; stratify by recording year and observable channel/quality metadata, documenting selection and sparse strata.
2. Choose exactly 20 historical canary episodes and run only decode/diarization/exemplars/three-encoder indexing for that bounded canary.
3. Prepare candidate excerpts/evaluation commands for P7-02 and extrapolate remaining local storage/runtime from measured canary results.

## Inputs, outputs and failure behavior

Released P6 and remaining archive; output historical manifest, stratified 20 canary, private voice artifacts and estimates. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Missing historical sources, unsupported strata or failed canary stages block the review packet.

## Exclusions

No remaining historical bulk run or historical ASR before approval.

## Acceptance checklist

- [ ] Historical/canary hashes and strata are fixed before label review or scoring.
- [ ] Exactly 20 canary episodes have voice-only artifacts; no ASR is required.
- [ ] Cross-year labeling packet and resource estimate are ready for human review.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run zanzara corpus freeze-historical --exclude "$ZANZARA_CORPUS_400" --canary-count 20 --output "$ZANZARA_HISTORICAL_MANIFEST"
uv run zanzara process --manifest "$ZANZARA_HISTORICAL_CANARY" --stage voice-index
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues/PRs contain sanitized aggregate results and synthetic examples only. Open a PR describing outcome, checks, limitations and evidence, then mark In review. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Missing historical sources, unsupported strata or failed canary stages block the review packet. Complete only after the required PR is merged and every acceptance item is satisfied; a draft/open PR alone is not completion. Keep [P7](P7.md) open for Sol review and user release.
