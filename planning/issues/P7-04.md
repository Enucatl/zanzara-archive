<!-- zanzara-plan:P7-04 -->
# [P7-04] Resumable historical voice index and ASR-missing appearances

- Stable ID: P7-04
- Executor: Luna
- Kind: Implementation
- Parent: P7
- Blocked by: P6, P7-03

## Outcome and requirement

Approved remaining historical manifest; output full historical voice collections and honest appearance pages.

## Design and interfaces

Use [D4](../SYSTEM-DESIGN.md#d4--worker-and-invalidation), [D7](../SYSTEM-DESIGN.md#d7--voice-retrieval-and-identity), [D10](../SYSTEM-DESIGN.md#d10--website-and-api), [D12](../SYSTEM-DESIGN.md#d12--expansion-and-documentation) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P7](P7.md). Required completed blockers: [P6](P6.md), [P7-03](P7-03.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Run voice-only stages through resumable jobs, reusing completed canary artifacts.
2. Publish validated voice generations incrementally and expose historical audio/candidates with transcript-unavailable state.
3. Report per-year coverage, insufficient clean speech, failures/resources and relevant regressions for Sol review.

## Inputs, outputs and failure behavior

Approved remaining historical manifest; output full historical voice collections and honest appearance pages. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Resource exhaustion, unaccounted episodes or retrieval regression blocks completion.

## Exclusions

No historical transcription queue execution or automatic phase release.

## Acceptance checklist

- [ ] Every historical manifest entry is accounted for and index generations remain consistent.
- [ ] No ASR job is a prerequisite for voice retrieval or appearance playback.
- [ ] Pending failures or drift are explicit release blockers.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run zanzara process --manifest "$ZANZARA_HISTORICAL_MANIFEST" --stage voice-index
uv run zanzara acceptance run --manifest "$ZANZARA_HISTORICAL_MANIFEST" --voice-only --output "$ZANZARA_HISTORICAL_COVERAGE"
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues/PRs contain sanitized aggregate results and synthetic examples only. Open a PR describing outcome, checks, limitations and evidence, then mark In review. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Resource exhaustion, unaccounted episodes or retrieval regression blocks completion. Complete only after the required PR is merged and every acceptance item is satisfied; a draft/open PR alone is not completion. Keep [P7](P7.md) open for Sol review and user release.
