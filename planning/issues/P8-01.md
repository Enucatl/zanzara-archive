<!-- zanzara-plan:P8-01 -->
# [P8-01] Prioritized remaining transcription queue and estimates

- Stable ID: P8-01
- Executor: Luna
- Kind: Implementation
- Parent: P8
- Blocked by: P7

## Outcome and requirement

Released P7, missing-ASR episodes and reviewed recurrence; output deterministic queue manifest and resource forecast.

## Design and interfaces

Use [D4](../SYSTEM-DESIGN.md#d4--worker-and-invalidation), [D12](../SYSTEM-DESIGN.md#d12--expansion-and-documentation) and the existing contracts/modules produced by completed prerequisites. The implementation issue establishes only interfaces within this scope; preserve compatible earlier contracts. See [execution rules](../GITHUB-SETUP.md#g7--luna-implementation-handoff-and-release-rules) for selection and evidence.

## Prerequisites

Parent: [P8](P8.md). Required completed blockers: [P7](P7.md). The preceding phase must have a current Sol PASS and explicit user release. This issue does not depend on its own parent closing.

## Bounded steps

1. Order missing transcription by explicit user priorities, then reviewed recurring-speaker appearances, then newest date and filename.
2. Freeze queue/source hashes and initial ten-episode batch boundaries; calculate incremental ASR/attribution/text-index/storage/backup requirements.
3. Reuse existing diarization/voice artifacts; record any stale dependency requiring human reconciliation.

## Inputs, outputs and failure behavior

Released P7, missing-ASR episodes and reviewed recurrence; output deterministic queue manifest and resource forecast. Preserve source/model/configuration provenance in outputs and fail without publishing partial artifacts when validation fails. Missing source/provenance or insufficient estimate evidence blocks approval packet.

## Exclusions

No transcription execution before approval or voice re-indexing merely because ASR changes.

## Acceptance checklist

- [ ] Every missing-ASR episode appears once and order is deterministic with documented priority inputs.
- [ ] Estimates exclude already-completed work and identify capacity/uncertainty.
- [ ] Required evidence below is linked and applies to the reviewed commit/configuration.

## Verification commands

```bash
uv run zanzara transcription plan --batch-size 10 --output "$ZANZARA_TRANSCRIPTION_QUEUE"
uv run zanzara estimate --manifest "$ZANZARA_TRANSCRIPTION_QUEUE"
```

Add the named focused tests/CLI commands in this issue if they do not yet exist; P0-01 establishes common uv/Ruff/pytest commands. Run relevant CPU checks plus the explicitly required real GPU/integration/evaluation actions. Do not treat a mocked model response as measured model quality.

## Evidence and documentation

Record exact commands/results, reviewed commit, artifact/source/model/split hashes where applicable, and the private evidence path. Update the relevant interface/runbook documentation for implemented behavior. Public issues and comments contain sanitized aggregate results and synthetic examples only. Commit the bounded change directly to `main`, push `origin/main`, verify the remote commit, and comment with outcome, checks, limitations and evidence. Set this child issue to Done only after all acceptance items pass. For execution-only evaluation, commit the sanitized report/runbook changes and link private detailed artifacts.

## Stop conditions and completion rule

Missing source/provenance or insufficient estimate evidence blocks approval packet. Complete only after the required commit is pushed to `main` and every acceptance item is satisfied; a local or unpushed commit is not completion. Keep [P8](P8.md) open for Sol review and user release.
