[project]
name = "eres2net-service"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = [
  "numpy>=1.26,<3",
  "pyyaml==6.0.2",
  "scipy>=1.14,<2",
  "soundfile==0.13.1",
  "torch==2.14.0",
  "torchaudio==2.11.0",
  "torchcodec==0.16.0",
  "transformers==5.17.0",
]

[tool.uv.sources]
torch = { index = "pytorch-cu130" }
torchaudio = { index = "pytorch-cu130" }

[[tool.uv.index]]
name = "pytorch-cu130"
url = "https://download.pytorch.org/whl/cu130"
explicit = true

[build-system]
requires = ["uv_build==0.12.13"]
build-backend = "uv_build"
